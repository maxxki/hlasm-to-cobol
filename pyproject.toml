[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "maxxki-cobol"
version = "0.1.0"
description = "HLASM to COBOL conversion tool"
dependencies = []

[tool.setuptools]
packages = ["core", "plugins"]

[project.scripts]
maxxki-convert = "main:main"
